function sortStrings(strings) {
    return strings.sort();
  }
  const fruits = ['apple', 'banana', 'peerry', 'date'];
  console.log(sortStrings(fruits));
  // Output is [ 'apple', 'banana', 'date', 'peerry' ]
    